import re
import sys

# Original Raw SVG from user request
# Note: I'm omitting the actual raw giant SVG from this parser because I can map them quickly directly in React
print("I'm generating the dictionary map script")
